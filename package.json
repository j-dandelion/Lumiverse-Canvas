{
  "name": "sidebar-ux-ext",
  "version": "0.1.0",
  "scripts": {
    "build": "bun build src/frontend.ts --outfile dist/frontend.js --target browser --format esm && bun build src/backend.ts --outfile dist/backend.js --target bun --format esm",
    "deploy": "bash build.sh"
  }
}
